name: zerotierapi
description: A ZeroTier device status Flutter app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5
  shared_preferences: ^2.0.15
  intl: ^0.18.0
  flutter_local_notifications: ^15.0.0
  sqflite_common_ffi: ^2.3.0
  sqflite_common_ffi_web: ^0.4.2
  path: ^1.8.0
  sqflite: ^2.4.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true